const fs = require('fs')

exports.router = (ctx) => {
  routerActionMap[]
}

const routerActionMap = {
  '/': 'rootRouter',
  '.js': '',
  '.vue': ''
}

/**
 * @description 匹配根路径，返回入口文件，没错，vite的入口文件不是js，而是一个html文件
 */
const rootRouter = (ctx) => {
  const htmlCode = 
}